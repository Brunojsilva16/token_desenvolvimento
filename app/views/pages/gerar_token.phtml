<?php // app/views/pages/gerar_token.phtml ?>
<div class="container-fluid">
    <h2 class="mb-4 text-gray-800">Novo Atendimento</h2>

    <?php if (isset($success) && $success): ?>
        <div class="alert alert-success alert-dismissible fade show">
            <?= $success ?>
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    <?php endif; ?>

    <?php if (isset($error) && $error): ?>
        <div class="alert alert-danger alert-dismissible fade show">
            <?= $error ?>
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    <?php endif; ?>

    <form action="<?= URL_BASE ?>/gerar-token/salvar" method="POST" autocomplete="off">
        
        <div class="row">
            <div class="col-lg-8 coluna-70">
                <div class="card shadow mb-4">
                    <div class="card-header py-3">
                        <h6 class="m-0 fw-bold text-primary">Dados do Atendimento</h6>
                    </div>
                    <div class="card-body">
                        <!-- 1. Identificação -->
                        <h6 class="text-muted mb-3 small text-uppercase fw-bold">1. Identificação</h6>
                        <div class="row">
                            <div class="col-md-7 mb-3">
                                <label class="form-label fw-bold">Paciente *</label>
                                <div class="input-group">
                                    <span class="input-group-text"><i class="fas fa-search"></i></span>
                                    <!-- Input de Busca -->
                                    <input type="text" class="form-control" id="nome_paciente" name="nome_paciente" 
                                           required placeholder="Buscar paciente cadastrado..." list="lista_pacientes" autocomplete="off">
                                    <datalist id="lista_pacientes"></datalist>
                                </div>
                                <small class="text-muted fst-italic" style="font-size: 0.8rem;">
                                    * Selecione da lista para preencher os dados automaticamente.
                                </small>
                            </div>
                            <div class="col-md-5 mb-3">
                                <label class="form-label fw-bold">Profissional *</label>
                                <select class="form-select" name="profissional_id" required>
                                    <option value="">Selecione...</option>
                                    <?php foreach ($profissionais as $prof): ?>
                                        <option value="<?= $prof['id'] ?>"><?= htmlspecialchars($prof['nome']) ?></option>
                                    <?php endforeach; ?>
                                </select>
                            </div>
                        </div>

                        <div class="row">
                            <div class="col-md-4 mb-3">
                                <label class="form-label">CPF</label>
                                <input type="text" class="form-control bg-light" id="cpf_paciente" name="cpf_paciente" readonly>
                            </div>
                            <div class="col-md-4 mb-3">
                                <label class="form-label">Nome Responsável</label>
                                <input type="text" class="form-control" id="nome_responsavel" name="nome_responsavel">
                            </div>
                            <div class="col-md-4 mb-3">
                                <label class="form-label">Resp. Financeiro</label>
                                <input type="text" class="form-control" id="responsavel_financeiro" name="responsavel_financeiro">
                            </div>
                        </div>

                        <hr>

                        <!-- 2. Financeiro -->
                        <h6 class="text-muted mb-3 small text-uppercase fw-bold">2. Financeiro</h6>
                        <div class="row">
                            <div class="col-md-3 mb-3">
                                <label class="form-label fw-bold text-success">Valor (R$)</label>
                                <input type="text" class="form-control fw-bold border-success" name="valor" id="valor" placeholder="0,00" required>
                            </div>
                            <div class="col-md-3 mb-3">
                                <label class="form-label">Pagamento</label>
                                <select class="form-select" name="formapag" required>
                                    <option value="DINHEIRO">Dinheiro</option>
                                    <option value="PIX">PIX</option>
                                    <option value="CARTAO_DEBITO">Débito</option>
                                    <option value="CARTAO_CREDITO">Crédito</option>
                                    <option value="BOLETO">Boleto</option>
                                </select>
                            </div>
                            <div class="col-md-3 mb-3">
                                <label class="form-label">Nome do Banco</label>
                                <input type="text" class="form-control" name="nome_banco" placeholder="Ex: Nubank">
                            </div>
                            <div class="col-md-3 mb-3">
                                <label class="form-label">Modalidade</label>
                                <select class="form-select" name="modalidadep">
                                    <option value="PARTICULAR">Particular</option>
                                    <option value="CONVENIO">Convênio</option>
                                    <option value="RETORNO">Retorno</option>
                                </select>
                            </div>
                        </div>
                        
                        <!-- Linha Vencimento e Origem (Radio Buttons) -->
                        <div class="row align-items-end bg-light p-2 rounded mx-1">
                            <div class="col-md-3 mb-2">
                                <label class="form-label fw-bold">Vencimento</label>
                                <input type="date" class="form-control" name="vencimento" value="<?= date('Y-m-d') ?>">
                            </div>
                            
                            <!-- RADIO BUTTONS ORIGEM -->
                            <div class="col-md-9 mb-2">
                                <label class="form-label d-block fw-bold text-muted small text-uppercase">Origem do Paciente</label>
                                <div class="d-flex align-items-center">
                                    <div class="form-check form-check-inline me-4">
                                        <input class="form-check-input" type="radio" name="origem" value="Organico" id="origem_organico">
                                        <label class="form-check-label" for="origem_organico">
                                            <i class="fas fa-leaf text-success me-1"></i> Orgânico
                                        </label>
                                    </div>
                                    <div class="form-check form-check-inline">
                                        <input class="form-check-input" type="radio" name="origem" value="Marketing" id="origem_marketing">
                                        <label class="form-check-label" for="origem_marketing">
                                            <i class="fas fa-bullhorn text-info me-1"></i> Marketing
                                        </label>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Agendamento -->
            <div class="col-lg-4 coluna-30">
                <div class="card shadow mb-4 border-left-info">
                    <div class="card-header py-3 d-flex justify-content-between align-items-center">
                        <h6 class="m-0 fw-bold text-info"><i class="fas fa-calendar-alt me-1"></i> Agendamento</h6>
                        <button type="button" class="btn btn-sm btn-info text-white" id="btnAddSessao">
                            <i class="fas fa-plus"></i> Add
                        </button>
                    </div>
                    <div class="card-body bg-light">
                        <div id="container-sessoes">
                            <div class="mb-2 input-group sessao-item">
                                <span class="input-group-text bg-white text-info fw-bold">1ª</span>
                                <input type="date" class="form-control" name="sessoes[]">
                            </div>
                        </div>
                        <div class="mt-3 text-center">
                            <small class="text-muted fst-italic">As datas serão salvas automaticamente.</small>
                        </div>
                    </div>
                </div>

                <div class="d-grid">
                    <button type="submit" class="btn btn-primary btn-lg py-3 shadow">
                        <i class="fas fa-check-circle me-2"></i> Confirmar Tudo
                    </button>
                </div>
            </div>
        </div>
    </form>
</div>

<!-- Scripts -->
<script>
    // --- 1. Script de Autocomplete (Focado apenas em Pacientes Cadastrados) ---
    const inputNome = document.getElementById('nome_paciente');
    const inputCpf = document.getElementById('cpf_paciente');
    const inputResp = document.getElementById('nome_responsavel');
    const inputFin = document.getElementById('responsavel_financeiro');
    const datalist = document.getElementById('lista_pacientes');
    
    // Mapa de memória
    let pacientesMap = {}; 
    let timeout = null;

    if (inputNome) {
        inputNome.addEventListener('input', function() {
            const termo = this.value;
            
            // 1. Tenta encontrar no mapa de memória
            if (pacientesMap[termo]) {
                const p = pacientesMap[termo];
                inputCpf.value = p.cpf || '';
                inputResp.value = p.resp || '';
                inputFin.value = p.fin || '';
                
                // Marca o Radio Button
                const radios = document.getElementsByName('origem');
                radios.forEach(r => r.checked = false);

                if (p.origem) {
                    for (let r of radios) {
                        if (r.value.toLowerCase() === p.origem.toLowerCase()) {
                            r.checked = true;
                            break;
                        }
                    }
                }

                inputNome.style.borderColor = '#1cc88a'; 
                return;
            } else {
                 inputNome.style.borderColor = ''; 
            }

            // 2. Busca API
            clearTimeout(timeout);
            if (termo.length < 3) return;

            timeout = setTimeout(() => {
                fetch('<?= URL_BASE ?>/api/pacientes/busca?term=' + encodeURIComponent(termo))
                    .then(response => {
                        if (!response.ok) throw new Error('Erro na requisição');
                        return response.json();
                    })
                    .then(data => {
                        datalist.innerHTML = ''; 
                        pacientesMap = {}; 

                        if (Array.isArray(data)) {
                            data.forEach(p => {
                                pacientesMap[p.nome] = p;

                                const option = document.createElement('option');
                                option.value = p.nome;
                                // Visual limpo: Nome e CPF apenas
                                const cpfLabel = p.cpf ? ` (CPF: ${p.cpf})` : '';
                                option.label = `${p.nome}${cpfLabel}`;
                                
                                datalist.appendChild(option);
                            });
                        }
                    })
                    .catch(error => console.error('Erro na busca:', error));
            }, 300);
        });
    }

    // --- 2. Adicionar Sessões ---
    const btnAdd = document.getElementById('btnAddSessao');
    const container = document.getElementById('container-sessoes');
    let contador = 1;
    if (btnAdd) {
        btnAdd.addEventListener('click', function() {
            contador++;
            const div = document.createElement('div');
            div.className = 'mb-2 input-group sessao-item fade-in';
            div.innerHTML = `
                <span class="input-group-text bg-white text-secondary fw-bold">${contador}ª</span>
                <input type="date" class="form-control" name="sessoes[]">
                <button type="button" class="btn btn-outline-danger btn-remove-sessao"><i class="fas fa-times"></i></button>
            `;
            container.appendChild(div);
        });
        container.addEventListener('click', e => {
            if(e.target.closest('.btn-remove-sessao')) e.target.closest('.sessao-item').remove();
        });
    }

    // --- 3. Máscara Valor ---
    const inputVal = document.getElementById('valor');
    if(inputVal) inputVal.addEventListener('input', e => {
        let v = e.target.value.replace(/\D/g,'');
        v = (v/100).toFixed(2)+'';
        v = v.replace(".",",").replace(/(\d)(?=(\d{3})+(?!\d))/g, "$1.");
        e.target.value = v;
    });
</script>

<style>
    .fade-in { animation: fadeIn 0.3s ease-in; }
    @keyframes fadeIn { from { opacity: 0; transform: translateY(-5px); } to { opacity: 1; transform: translateY(0); } }
    @media (min-width: 992px) { .coluna-70 { width: 70%; } .coluna-30 { width: 30%; } }
</style>