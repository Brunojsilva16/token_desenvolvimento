<?php // app/Views/pages/print_token.phtml ?>
<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <title>Imprimir Token</title>
    <style>
        body {
            font-family: 'Courier New', Courier, monospace; /* Fonte tipo cupom fiscal */
            width: 300px; /* Largura típica de impressora térmica */
            margin: 0 auto;
            padding: 20px;
            text-align: center;
        }
        .header {
            margin-bottom: 20px;
            border-bottom: 1px dashed #000;
            padding-bottom: 10px;
        }
        .token-box {
            border: 2px solid #000;
            padding: 10px;
            margin: 20px 0;
            font-size: 1.6rem;
            font-weight: bold;
        }
        .details {
            font-size: 14px;
            text-align: left;
            margin-bottom: 20px;
        }
        .footer {
            font-size: 12px;
            margin-top: 20px;
            border-top: 1px dashed #000;
            padding-top: 10px;
        }
        @media print {
            .no-print { display: none; }
        }
    </style>
</head>
<body onload="window.print()">

    <div class="header">
        <h3>CLÍNICA MÉDICA</h3>
        <p>Controle de Atendimento</p>
    </div>

    <div class="details">
        <p><strong>Paciente:</strong><br> <?= htmlspecialchars($token['nome_paciente']) ?></p>
        <p><strong>Profissional:</strong><br> <?= htmlspecialchars($token['nome_profissional'] ?? 'Geral') ?></p>
        <p><strong>Data:</strong> <?= date('d/m/Y H:i', strtotime($token['created_at'])) ?></p>
    </div>

    <div>SENHA:</div>
    <div class="token-box">
        <?= $token['token'] ?>
    </div>

    <div class="footer">
        <p>Aguarde ser chamado pelo painel.</p>
        <small>Gerado em: <?= date('d/m/Y H:i:s') ?></small>
    </div>

    <button class="no-print" onclick="window.print()" style="margin-top: 20px; padding: 10px;">Imprimir Novamente</button>
    <button class="no-print" onclick="window.close()" style="margin-top: 20px; padding: 10px;">Fechar</button>

</body>
</html>